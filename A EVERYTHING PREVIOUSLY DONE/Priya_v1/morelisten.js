// Scroll to the chart section on link click.
document.querySelector('#chart-section-link').addEventListener('click', () => {
document.querySelector('#chart-section').scrollIntoView({ behavior: 'smooth' });
});
